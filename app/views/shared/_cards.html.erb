<div class="row">
  <% events.each do |event| %>
    <div class="card card-custom">
      <div class="row">

        <div class="col-3">
          <div class="thumb-image-trim">
            <%= image_tag event.main_image, class: "thumb-image" %>            
          </div>
        </div>

        <div class="col-7 content-center">
          <h3><%= link_to truncate(event.title.titleize, length: 25), event, class: "card-heading" %></h3>
          <hr>
          <span style="color: #3AE39F">When: &nbsp;&nbsp;</span><%= event.start.strftime("%A, %B %d, %Y")%> 
          <br>
          <span style="color: #3AE39F">Where: &nbsp;</span><%= event.location %>
        </div>

        <div class="col-2">
          <%= render partial: button_partial, locals: {event: event} unless button_partial.empty? %>
        </div>

      </div> <!-- Row -->
    </div> <!-- Card -->
  <% end %>
</div>